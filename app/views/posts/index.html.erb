<h1> This is the index</h1>

<p class="posts_index">
<% @posts.each do |post|%>
<div class="post_wrapper">
<h2 class ="blog_index_title"><%= link_to post.title, post %></h2>
<p class="date"><%= post.created_at.strftime("%B, %d, %Y") %></p></div>
<% end %>